{
  "name": "dz5",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npx parcel ./src/*",
    "build": "parcel build ./src/* --no-cache --public-url /",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist",
    "re": "npm run predeploy && git add . && git commit -m main && git push origin main && npm run deploy"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "parcel": "^2.16.0",
    "pnotify": "^5.2.0"
  }
}
